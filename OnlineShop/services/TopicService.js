
var Q = require('q');
var logger = require('./LogService');

var subscribeQueue = function(aws, queue, topicName){
	
};

var sendObject = function(topicName, obj) {

};

module.exports = {
	subscribe: subscribeQueue,
	send: sendObject
};